[db]
user="dev"
database="Development"
host="localhost"
port=5432
prefix="postgresql+psycopg2"
schema="data_organization_testing"

[table_settings]
mandatory_columns = ["ctype"]
optional_columns = ["is_primary", "is_unique", "nullable"]
[table_settings.key_types]
    ctype="str"
    is_primary="bool"
    is_unique="bool"
    nullable="bool"
